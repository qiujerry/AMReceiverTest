This is the py_server-write-a-block package meant as a guide to building
out-of-tree packages. To use the py_server blocks, the Python namespaces
is in 'py_server', which is imported as:

    import py_server

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using:

    help(py_server)
